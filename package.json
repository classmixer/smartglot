{
  "name": "smartglot",
  "version": "1.0.0",
  "description": "Static site for Firebase App Hosting",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "cd smartglot-react && npm run build",
    "gcp-build": "cd smartglot-react && npm install && npm run build && cd .. && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@elastic/elasticsearch": "^9.0.1",
    "dotenv": "^16.5.0",
    "express": "^4.19.2"
  }
}
